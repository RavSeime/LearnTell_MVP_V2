# Additional dependencies for LangGraph Interview Agent
# Add these to your existing requirements.txt or install via: pip install -r langgraph_requirements.txt

langchain>=0.3.0
langchain-openai>=0.2.0
langgraph>=0.2.0
langsmith>=0.1.0
openai>=1.12.0

# Optional: for better async support
aiohttp>=3.9.0
